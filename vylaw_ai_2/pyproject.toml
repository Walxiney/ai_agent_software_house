[project]
name = "vylaw_ai_2"
version = "0.1.0"
description = "vylaw_ai_2 using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.13"
dependencies = [
    "crewai[tools]>=0.108.0,<1.0.0",
]

[project.scripts]
kickoff = "vylaw_ai_2.main:kickoff"
plot = "vylaw_ai_2.main:plot"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"

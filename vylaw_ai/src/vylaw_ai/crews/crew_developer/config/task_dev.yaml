develop_python_code:
  description: >
    You will create a game using python, these are the instructions:

    Instructions
    ------------
    {project_description}
    
    Use o feedback (se houver) do Python Validator para ajustar erros encontrados no código, Obrigatóriamente faça apenas os ajustes solicitados nada mais.
    ------------
    {feedback}

    Use o seu código anterior como base para a alteração nunca recomece do zero apenas faça as alterações necessárias.
    ------------
    {code}
  expected_output: >
    Your Final answer must be the full python code, only the python code and nothing else.
  agent: python_developer

# develop_python_code:
#   description: >
#     Desenvolver um código em Python que atenda aos requisitos específicos do projeto {project_name}.

#     Contador de retrabalho: {retry}

#     **Descrição da tarefa:**
#     {project_description}

#     Requisitos obrigatórios:
#     - O código deve ser 100% funcional e se possivel testado antes de ser entregue.
#     - O código deve ser limpo, bem estruturado e seguir as melhores práticas de programação.
#     - O código deve ser bem comentado e conter docstrings explicativas.
#     - O código deve ser eficiente e otimizado para desempenho.
#     - O código final deve ser apenas o código sem pontuações aspas ou explicações que não fazem parte do código.

#     Use o feedback (se houver) do Python Validator para ajustar erros encontrados no código, Obrigatóriamente faça apenas os ajustes solicitados nada mais.
#     {feedback}

#     Use o seu código anterior como base para a alteração nunca recomece do zero apenas faça as alterações necessárias.
#     {code}

#   expected_output: >
#     Apenas o código-fonte Python, limpo e organizado pronto para ser executado.
#   agent: python_developer

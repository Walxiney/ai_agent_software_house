[project]
name = "software_house"
version = "0.1.0"
description = "software_house using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<=3.13"
dependencies = [
    "crewai[tools]>=0.85.0,<1.0.0"
]

[project.scripts]
software_house = "software_house.main:run"
run_crew = "software_house.main:run"
train = "software_house.main:train"
replay = "software_house.main:replay"
test = "software_house.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
